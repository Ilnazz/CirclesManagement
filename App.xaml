<Application x:Class="CirclesManagement.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CirclesManagement"
             xmlns:converters="clr-namespace:CirclesManagement.Converters"
             StartupUri="AuthorizationWindow.xaml">
    <Application.Resources>
        <converters:TimeConverter x:Key="TimeConverter"/>

        <GroupStyle x:Key="PupilsDataGridGroupStyle">
            <GroupStyle.ContainerStyle>
                <Style TargetType="{x:Type GroupItem}">
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type GroupItem}">
                                <Expander IsExpanded="True" Background="#457b9d" BorderBrush="#457b9d" Foreground="White" BorderThickness="5">
                                    <Expander.Header>
                                        <TextBlock TextAlignment="Right" HorizontalAlignment="Stretch">
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="{}Класс - {0}   учеников - {1}">
                                                    <Binding Path="Name"/>
                                                    <Binding Path="ItemCount"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </Expander.Header>
                                    <Expander.Content>
                                        <ItemsPresenter>
                                            <ItemsPresenter.Style>
                                                <Style>
                                                    <Setter Property="Control.Foreground" Value="Black"/>
                                                </Style>
                                            </ItemsPresenter.Style>
                                        </ItemsPresenter>
                                    </Expander.Content>
                                </Expander>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </GroupStyle.ContainerStyle>
        </GroupStyle>

        <GroupStyle x:Key="TimetableDataGridGroupStyle">
            <GroupStyle.ContainerStyle>
                <Style TargetType="{x:Type GroupItem}">
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type GroupItem}">
                                <Expander IsExpanded="True" Background="#457b9d" BorderBrush="#457b9d" Foreground="White" BorderThickness="5">
                                    <Expander.Header>
                                        <TextBlock Text="{Binding Name, StringFormat={}Расписание занятий на {0}}"
                                                   TextAlignment="Right" HorizontalAlignment="Stretch"/>
                                    </Expander.Header>
                                    <Expander.Content>
                                        <ItemsPresenter>
                                            <ItemsPresenter.Style>
                                                <Style>
                                                    <Setter Property="Control.Foreground" Value="Black"/>
                                                </Style>
                                            </ItemsPresenter.Style>
                                        </ItemsPresenter>
                                    </Expander.Content>
                                </Expander>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </GroupStyle.ContainerStyle>
        </GroupStyle>

        <GroupStyle x:Key="LessonDataGridGroupStyle">
            <GroupStyle.ContainerStyle>
                <Style TargetType="{x:Type GroupItem}">
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type GroupItem}">
                                <Expander IsExpanded="True" Background="#457b9d" BorderBrush="#457b9d" Foreground="White" BorderThickness="5">
                                    <Expander.Header>
                                        <TextBlock Text="{Binding Name}"
                                                   TextAlignment="Right" HorizontalAlignment="Stretch"/>
                                    </Expander.Header>
                                    <Expander.Content>
                                        <ItemsPresenter>
                                            <ItemsPresenter.Style>
                                                <Style>
                                                    <Setter Property="Control.Foreground" Value="Black"/>
                                                </Style>
                                            </ItemsPresenter.Style>
                                        </ItemsPresenter>
                                    </Expander.Content>
                                </Expander>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </GroupStyle.ContainerStyle>
        </GroupStyle>

        <!--<Style x:Key="BackgroundMain">
            <Setter Property="Control.Background" Value="#f9f9f9"/>
        </Style>
        <Style x:Key="BackgroundAdditional">
            <Setter Property="Control.Background" Value="#7fc8f8"/>
        </Style>
        <Style x:Key="BackgroundAccent">
            <Setter Property="Control.Background" Value="#ffe45e"/>
        </Style>

        <Style x:Key="ForegroundMain">
            <Setter Property="Control.Background" Value="#f9f9f9"/>
        </Style>
        <Style x:Key="ForegroundAdditional">
            <Setter Property="Control.Background" Value="#7fc8f8"/>
        </Style>
        <Style x:Key="ForegroundAccent">
            <Setter Property="Control.Background" Value="#ffe45e"/>
        </Style>-->

        <Style x:Key="CommonStyle">
            <Setter Property="Control.FontFamily" Value="Century Schoolbook" />
            <Setter Property="Control.FontSize" Value="16" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="BorderThickness" Value="0.75"/>
            <Setter Property="Padding" Value="5,2.5,5,2.5"/>
            <Setter Property="Background" Value="#a8dadc"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>
        
        <!--<Style TargetType="TextBox">
            <Setter Property="Padding" Value="1" />
        </Style>
        
        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="1" />
        </Style>-->

        <Style x:Key="DGTextColumnStyle" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        
        <Style x:Key="DGTextColumnEditingStyle" TargetType="TextBox">
        </Style>
        
        <Style x:Key="DGCheckBoxColumnStyle" TargetType="CheckBox">
        </Style>
    </Application.Resources>
</Application>
