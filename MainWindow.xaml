<Window x:Class="CirclesManagement.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CirclesManagement"
        mc:Ignorable="d"
        Title="Управление кружками" Height="600" Width="1000" Style="{StaticResource CommonStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Style="{StaticResource BackgroundAdditional}">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <!--<Image Source="/Resources/school_logo.png" Width="25" Margin="5" HorizontalAlignment="Left"/>-->
                <TextBlock Text="Школа 'Олимп'" VerticalAlignment="Center" TextWrapping="Wrap"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                <Button x:Name="BLogOut" Content="Выйти" Click="BLogOut_Click" Margin="5"/>
                <Button x:Name="BGoToPreviousPage" Content="Назад" Click="BGoToPreviousPage_Click" Margin="5"/>
            </StackPanel>
        </DockPanel>
        <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" Grid.Row="1" Margin="5"/>
    </Grid>
</Window>
